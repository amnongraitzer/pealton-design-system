---
phase: quick-260719-d9g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/TABLE-CONTRACT.md]
autonomous: true
requirements: [PLT-079]
must_haves:
  truths:
    - "docs/TABLE-CONTRACT.md exists as a pure Markdown doc, no code touched"
    - "All 12 required sections present in exact set: States, Keyboard, Selection, Inline edit, Bulk actions, Assign, Progressive loading + server search, Concurrency, Confirm ladder, Table inventory, Changelog, Open questions"
    - "Every behaviour statement phrased GIVEN/WHEN/THEN and individually spot-checkable"
    - "Stage optimistic-sync nuance recorded correctly (stage keeps optimistic table sync; everything else wait-not-optimistic)"
    - "All 8 table surfaces mapped to adoption task ids (PLT-084..088) with target 0 exemptions"
    - "Real-estate app tables noted OUT of scope; PLT-089 noted as later reconciler"
  artifacts:
    - path: "docs/TABLE-CONTRACT.md"
      provides: "Written table behaviour contract for Epic 3"
      contains: "GIVEN"
      min_lines: 180
  key_links: []
---

<objective>
Create `docs/TABLE-CONTRACT.md` — the single written behaviour contract for Epic 3
(PLT-079). It captures every table behaviour decision from PLT-077 (Amnon's grill-me
decisions) plus the current console.js behaviour that must be documented where it is kept.

Purpose: give every downstream adoption task (PLT-080..088) one authoritative,
individually-testable spec so implementers do not re-litigate decisions.
Output: one new Markdown file. No code, no other file touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260719-d9g-create-docs-table-contract-md-written-ta/260719-d9g-INPUTS.md
@./CLAUDE.md

# INPUTS.md is the ONLY normative source. Do NOT re-fetch the Pealton HR repo,
# BACKLOG.md, or console.js — everything needed is already extracted in INPUTS.md.
# docs/index.html may be skimmed once for house tone only (optional).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the behaviour sections (States → Confirm ladder)</name>
  <files>docs/TABLE-CONTRACT.md</files>
  <action>
Create docs/TABLE-CONTRACT.md. Open with a short intro: what this contract is (the
Epic 3 / PLT-079 table behaviour spec), that it draws ONLY on INPUTS.md sources
(PLT-077 decisions + current console.js), that every statement is GIVEN/WHEN/THEN and
individually testable, and a scope note: real-estate app tables are OUT of scope
(future adopters only), and PLT-089 later reconciles this doc to as-built.

Then write these sections, in this order, each behaviour as a GIVEN/WHEN/THEN bullet:

- **States** — cover all four: loading (skeleton), empty (per-view copy), error
  (retry affordance), partial (progressive-load in flight).
- **Keyboard** — row focus/arrow movement; Enter opens drawer; Esc rules; inline-edit
  keys (Enter = confirm, Esc = cancel).
- **Selection** — persists across progressive loads AND filter/search changes; bar
  shows "N selected" INCLUDING now-hidden rows + a clear-selection affordance; bulk
  applies to the FULL selection, never just visible rows. Note today's console.js
  basis (Tabulator selectableRows / getSelectedData → application_ids, "N נבחרו",
  deselectRow clear) is kept and extended.
- **Inline edit** — fields stage/owner/role_title. Inline edits AND drawer edits share
  ONE grammar: per-field commit, explicit ✓/✕, busy state in-control, wait-not-optimistic
  EXCEPT stage's existing optimistic table sync (kept + documented). State explicitly:
  stage keeps its optimistic table.updateData sync WITHOUT full reload; everything else
  waits for the server (not optimistic). Note the console.js rule that a silent
  background refresh must not yank an open edit form out mid-typing.
- **Bulk actions** — the 4-action bulk bar: branch-add (exists) + stage-change +
  take-ownership + branch-remove. Note branch-add is add-only today (three-outcome
  report added/already/skipped); branch-remove is the new destructive action gated by
  the confirm ladder (Task-2 inventory ties to PLT-085/086). Partial failure: per-item
  outcomes done/already/skipped/failed; failures KEEP their rows selected. Note this
  generalizes the existing bulk-add three-outcome pattern.
- **Assign** — three distinct semantics: console/hil_queue = candidate→branch
  (multi-select, UI max 4, ACTIVE branches only, UNASSIGNED candidates today);
  admin_users = user→role + branches; merge_queue = person-pair merge DECISION, NOT an
  assign — bulk actions stay OFF merge_queue.
- **Progressive loading + server search** — NO pagination; progressive scroll loading
  with SERVER-SIDE search/filter + exact counts (dedicated /api/candidates/counts,
  PLT-082). Note the current once-load (GET /api/candidates client-side, in-memory
  counts) is being REPLACED by server progressive load (PLT-082/083).
- **Concurrency** — writes carry the row's updated_at; stale write → 409 → row
  refreshes + inline notice, user re-applies; no silent overwrite.
- **Confirm ladder** — normal = ✓/✕; destructive single = confirm dialog; destructive
  bulk (branch-remove, terminal-negative stage) = dialog with exact count +
  typed-count confirmation.

Do NOT include Table inventory, Changelog, or Open questions here — those are Task 2.
Pure Markdown; no code fences containing implementation. Use CLAUDE.md house rules
(RTL/Hebrew-first phrasing where labels are Hebrew, e.g. "N נבחרו").
  </action>
  <verify>
    <automated>test -f docs/TABLE-CONTRACT.md && grep -qi 'GIVEN' docs/TABLE-CONTRACT.md && for s in States Keyboard Selection 'Inline edit' 'Bulk actions' Assign 'Progressive loading' Concurrency 'Confirm ladder'; do grep -qi "$s" docs/TABLE-CONTRACT.md || { echo "MISSING: $s"; exit 1; }; done && echo OK</automated>
  </verify>
  <done>File exists with the 9 behaviour sections above, all statements GIVEN/WHEN/THEN, stage optimistic nuance recorded, scope + PLT-089 note present.</done>
</task>

<task type="auto">
  <name>Task 2: Add Table inventory, Changelog, Open questions + DoD self-check</name>
  <files>docs/TABLE-CONTRACT.md</files>
  <action>
Append to docs/TABLE-CONTRACT.md:

- **Table inventory (adopt/exempt)** — a table mapping ALL 8 surfaces to their adoption
  task id, exactly per INPUTS.md:
    | Surface | Kind | Adoption task |
    | console (operator track) | candidate pool, full CRUD/assign | PLT-084 |
    | console (מטה / mgmt track) | HQ candidate pool | PLT-084 |
    | hil_queue | legacy assign queue | PLT-087 |
    | mgmt_track | legacy | PLT-087 |
    | branch_view | legacy read-mostly | PLT-087 |
    | admin_users | legacy user→role+branches | PLT-088 |
    | merge_queue (table 1) | legacy person-pair merge | PLT-088 |
    | merge_queue (table 2) | legacy person-pair merge | PLT-088 |
  State target: 0 exemptions; any exemption row requires a written reason (currently none).
- **Changelog** — a dated first entry: 2026-07-19, initial contract authored from
  PLT-077 decisions + current console.js (PLT-079). Note PLT-089 will reconcile this
  doc to as-built.
- **Open questions** — capture any genuinely unresolved items surfaced by INPUTS.md
  (e.g. the hunt double-fire bug closes via PLT-084 — track as a verification case, not
  an open decision). If none remain open, state "None open — all PLT-077 decisions
  confirmed 2026-07-19" rather than inventing questions.

Then run a self-check against the Definition-of-Done list below and fix any gap before
finishing. Every one of these must be traceable to a GIVEN/WHEN/THEN statement in the doc:
  * States: loading/empty/error/partial all covered.
  * Keyboard: row focus/arrow, Enter=drawer, Esc rules, inline keys.
  * Selection: persists across loads + filter/search; "N selected" incl. hidden + clear; bulk = full selection.
  * Inline+drawer: one grammar, per-field commit, ✓/✕, busy in-control, wait-not-optimistic EXCEPT stage optimistic sync.
  * Concurrency: updated_at + 409 refresh + re-apply, no silent overwrite.
  * Confirm ladder: ✓/✕ → confirm dialog → typed-count dialog with exact count.
  * Partial failure: done/already/skipped/failed; failures keep selection; generalizes bulk-add three-outcome.
  * Inventory: all 8 surfaces → PLT-084..088; exemptions need written reason (target none).
  * Assign: console/hil_queue candidate→branch (max 4); admin_users user→role+branches; merge_queue = merge decision, bulk OFF.
  </action>
  <verify>
    <automated>for t in PLT-084 PLT-087 PLT-088 Changelog 'Open questions' 'Table inventory'; do grep -qi "$t" docs/TABLE-CONTRACT.md || { echo "MISSING: $t"; exit 1; }; done && grep -qi 'merge_queue' docs/TABLE-CONTRACT.md && grep -qi 'PLT-089' docs/TABLE-CONTRACT.md && echo OK</automated>
  </verify>
  <done>All 12 sections present; inventory maps 8 surfaces to PLT-084..088 with 0-exemption target; changelog + open questions present; DoD self-check passes with every item traceable to a GIVEN/WHEN/THEN statement.</done>
</task>

</tasks>

<verification>
- `test -f docs/TABLE-CONTRACT.md` — the one and only deliverable exists.
- `git status --porcelain` shows ONLY docs/TABLE-CONTRACT.md changed (docs-only guardrail).
- All 12 required section headings present.
- No code files touched; no runtime deps or build involved.
</verification>

<success_criteria>
- docs/TABLE-CONTRACT.md is spec-complete: 12 sections, every behaviour GIVEN/WHEN/THEN.
- Stage optimistic-sync nuance and wait-not-optimistic default both stated correctly.
- 8-surface inventory mapped to adoption task ids; real-estate out of scope; PLT-089 reconciler noted.
- Nothing outside docs/TABLE-CONTRACT.md changed.
</success_criteria>

<output>
Create `.planning/quick/260719-d9g-create-docs-table-contract-md-written-ta/260719-d9g-SUMMARY.md` when done.
</output>
